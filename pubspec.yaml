name: regen
description: Gamified Recycling System for USM
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  intl: ^0.19.0
  url_launcher: ^6.2.2
  image_picker: ^1.0.4
  qr_code_scanner: ^1.0.1
  percent_indicator: ^4.2.3
  # 🔴 ADD THIS LINE to install the core Firebase SDK
  firebase_core: ^2.24.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/